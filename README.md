# Docker
Para compilar las imágenes, ejecutar el siguiente comando desde la raíz del proyecto:
```
docker/build.sh
```

## Corriendo Docker
Cambia al directorio **docker** y ejecuta:
```
docker compose up -d
```
La base de datos se ejecutará en el puerto 5432, ollama en el 11434.
